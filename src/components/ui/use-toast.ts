
// Import from hooks instead of importing directly
import { useToast, toast, clearToasts } from "@/hooks/use-toast";

// Re-export for backward compatibility
export { useToast, toast, clearToasts };
