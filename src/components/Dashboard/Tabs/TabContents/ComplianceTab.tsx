
import { ComplianceTab as RefactoredComplianceTab } from "./compliance";

// Re-export the component with the same name for backward compatibility
export function ComplianceTab() {
  return <RefactoredComplianceTab />;
}
