
/**
 * @deprecated Use the unified MetadataPanel from '@/components/MetadataPanel' instead.
 * This re-export exists for backward compatibility and will be removed in a future version.
 */

// Re-export the unified MetadataPanel
import MetadataPanel from "@/components/MetadataPanel";
export { MetadataPanel };
export default MetadataPanel;
